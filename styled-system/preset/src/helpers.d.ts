/**
 * Changes the alpha channel of a color
 * @param color color value or CSS variable
 * @param percentage Alpha channel value
 * @returns
 */
export declare function alpha(color: string, percentage?: number | string): string;
export declare function rem(pixels: number): string;
