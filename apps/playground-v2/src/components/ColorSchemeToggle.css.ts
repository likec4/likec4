import { css as style } from '$styled-system/css'
import { rem } from '@mantine/core'

export const icon = style({
  width: rem(18),
  height: rem(18),
  __likec4NodeHovered: {
    opacity: 0.1,
  },
})
