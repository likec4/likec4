---
'@likec4/mcp': patch
---

Add `render-view` MCP App tool that renders a LikeC4 view as an interactive diagram (pan/zoom/fit) inline in MCP hosts that support MCP Apps, in addition to the existing text-only `read-view` tool.
