---
'@likec4/diagram': patch
---

Breadcrumb segments in the sequence outline are now clickable — each one jumps the walkthrough to the first step of that fragment, so you can step back out to the top of an enclosing `alt`, `try` or `loop` in one click.
