import type { HexColor } from '../../types';
export declare const lightValue: {
    elements: {
        fill: HexColor;
        stroke: HexColor;
        hiContrast: HexColor;
        loContrast: HexColor;
    };
    relationships: {
        line: HexColor;
        label: HexColor;
        labelBg: HexColor;
    };
};
export declare const darkValue: {
    elements: {
        fill: HexColor;
        stroke: HexColor;
        hiContrast: HexColor;
        loContrast: HexColor;
    };
    relationships: {
        line: HexColor;
        label: HexColor;
        labelBg: HexColor;
    };
};
