# Agent Instructions

- when your changes relate to views model and layout, you ensure that View-drifts detection/auto-applying (`packages/core/src/manual-layout`) are updated accordingly.
  - if you are not sure what leads to layout drifts or can be auto-applied, ask for confirmation.
