import { style } from '@vanilla-extract/css'

export const toolbar = style({
  position: 'absolute',
  top: '0.25rem',
  right: '1rem'
})

export const cssToolbarLeft = style({
  position: 'absolute',
  top: '0.25rem',
  left: '1rem'
})
