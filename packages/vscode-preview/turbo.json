{
  "$schema": "https://turbo.build/schema.json",
  "extends": [
    "//"
  ],
  "tasks": {
    "generate": {
      "outputs": [
        "src/generated-icons.tsx"
      ],
      "inputs": [
        "scripts/**",
        "package.json"
      ]
    }
  }
}
