import { rem } from '@mantine/core'
import { style } from '@vanilla-extract/css'

export const toolbarTitle = style({
  fontSize: rem(10),
  fontWeight: 600,
  cursor: 'default',
  userSelect: 'all',
})
