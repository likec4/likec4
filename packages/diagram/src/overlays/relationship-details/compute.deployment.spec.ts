import { describe, it } from 'vitest'

describe('computeEdgeDetailsViewData', () => {
  describe('deploymentRealtion', () => {
    it('adds endpoints and theirs ancestors from deployment model', ({ expect }) => {
    })
  })
})
