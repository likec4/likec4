import { createVar, keyframes, style } from '@vanilla-extract/css'
import { whereDark } from '../../theme-vars'

//
export const dependencyViewContainer = style({
  width: '100%',
  height: '100%'
  // position: 'absolute',
  // top: 20,
  // left: 20,
  // width: 'calc(100% - 40px)',
  // height: 'calc(100% - 40px)',
  // overflow: 'hidden',
})
