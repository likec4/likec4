import { createGlobalVar, createVar } from '@vanilla-extract/css'

export const textSize = createVar('text-size')
export const paddingSize = createVar('padding-size')
export const iconSize = createGlobalVar('likec4-icon-size')
