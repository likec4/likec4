import { themeToVars } from '@mantine/vanilla-extract'

export {
  easings,
  transitions,
  vars,
  whereDark,
  whereLight,
  xyvars,
} from './theme-vars.css'

export const mantine = themeToVars({})
