# `@likec4/react`

> [!WARNING]
> This package is intended for internal usage and not published to npm

This package is a bundled version of `@likec4/diagram`, with rollup type definitions.

The build artifacts are copied to the [`likec4`](https://www.npmjs.com/package/likec4) and available as `likec4/react`.
