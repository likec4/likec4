{
  "$schema": "https://turbo.build/schema.json",
  "extends": [
    "//"
  ],
  "tasks": {
    "build": {
      "dependsOn": [
        "^generate",
        "^build"
      ],
      "outputs": [
        "dist/**",
        "!**/*.metafile.json"
      ]
    }
  }
}
