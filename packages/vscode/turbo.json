{
  "$schema": "https://turbo.build/schema.json",
  "extends": [
    "//"
  ],
  "tasks": {
    "build": {
      "dependsOn": [
        "^generate",
        "@likec4/vscode-preview#build"
      ],
      "outputs": [
        "dist/**",
        "!**/*.metafile.json"
      ]
    }
  }
}
