{
  "$schema": "https://turbo.build/schema.json",
  "extends": [
    "//"
  ],
  "tasks": {
    "generate": {
      "outputs": [
        "src/meta.ts"
      ],
      "inputs": [
        "$TURBO_DEFAULT$",
        "!src/**"
      ],
      "outputLogs": "new-only"
    }
  }
}
