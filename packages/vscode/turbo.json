{
  "$schema": "https://turbo.build/schema.json",
  "extends": [
    "//"
  ],
  "tasks": {
    "generate": {
      "outputs": [
        "src/meta.ts"
      ],
      "inputs": [
        "package.json"
      ],
      "outputLogs": "new-only"
    },
    "build": {
      "dependsOn": [
        "generate",
        "^generate",
        "@likec4/config#build",
        "@likec4/core#build",
        "@likec4/vscode-preview#build"
      ],
      "outputs": [
        "dist/**",
        "data/config.schema.json",
        "!dist/src/**",
        "!dist/**/*.metafile.json"
      ],
      "outputLogs": "new-only"
    }
  }
}
