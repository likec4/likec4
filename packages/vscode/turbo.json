{
  "$schema": "https://turbo.build/schema.json",
  "extends": [
    "//"
  ],
  "tasks": {
    "generate": {
      "inputs": ["package.json"],
      "outputs": [
        "src/meta.ts"
      ]
    }
  }
}
