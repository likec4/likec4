{
  "$schema": "https://turbo.build/schema.json",
  "extends": [
    "//"
  ],
  "tasks": {
    "generate": {
      "dependsOn": [
        "^generate"
      ],
      "outputs": [
        "contrib/**",
        "src/generated/**",
        "src/generated-lib/**"
      ],
      "inputs": [
        "scripts/**",
        "src/**/*.langium",
        "langium-config.json",
        "package.json"
      ]
    }
  }
}
