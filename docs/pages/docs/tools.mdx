import { Card, Cards } from "nextra-theme-docs"
import meta from "./tools/_meta.json"

export const prefix = "/docs/tools"

# LikeC4 Tools

<Cards>
  {Object.entries(meta).map(([key, page]) => (
    <Card key={key} title={page.title} href={`${prefix}/${key}/`}/>
  ))}
</Cards>
