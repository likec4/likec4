import { Card, Cards } from "nextra/components"
import meta from "./dsl/_meta.json"

export const prefix = "/docs/dsl"

# LikeC4 Language

<Cards num={1}>
  {Object.entries(meta).map(([key, {title}], i)=> (
    <Card key={key} title={`${i+1}. ${title}`} href={`${prefix}/${key}/`} arrow/>
  ))}
</Cards>
