{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "compile": {
      "dependsOn": ["^build"],
      "inputs": [
        "src/**"
      ],
      "outputs": []
    },
    "build": {
      "outputMode": "new-only",
      "dependsOn": ["^build"],
      "inputs": [
        "src/**"
      ],
      "outputs": [
        "dist/**",
        "contrib/**"
      ]
    },
    "lint": {
      "outputs": []
    },
    "test": {
      "outputs": [],
      "dependsOn": ["build"]
    },
    "test:watch": {
      "cache": false
    },
    "clean": {
      "cache": false
    },
    "dev": {
      "cache": false,
      "persistent": true
    }
  }
}
