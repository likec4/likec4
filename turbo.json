{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "compile": {
      "dependsOn": ["^compile"],
      "outputs": [],
      "inputs": [
        "src/**",
        "tsconfig.json"
      ]
    },
    "build": {
      "outputMode": "new-only",
      "dependsOn": ["^build"],
      "inputs": [
        "src/**"
      ],
      "outputs": [
        "dist/**",
        "contrib/**"
      ]
    },
    "@likec4/docs-website#build": {
      "dependsOn": ["^build"],
      "outputs": [".next/**", "!.next/cache/**", "out/**"]
    },
    "build:release": {
      "dependsOn": ["^build:release"],
      "cache": false
    },
    "lint": {
      "outputs": []
    },
    "test": {
      "outputs": [],
      "dependsOn": ["build"]
    },
    "test:watch": {
      "outputs": [],
      "cache": false
    },
    "clean": {
      "cache": false
    },
    "dev": {
      "cache": false,
      "persistent": true
    }
  }
}
