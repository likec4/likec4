{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "generate": {},
    "typecheck": {
      "cache": false,
      "dependsOn": ["generate", "^build"],
      "outputs": []
    },
    "build": {
      "dependsOn": ["generate", "^build"],
      "outputs": ["dist/**", ".tsbuildinfo/**"]
    },
    "clean": {
      "cache": false
    }
  },
  "globalDependencies": [
    "tsconfig.*.json",
    "tsconfig.json",
    "yarn.lock"
  ]
}
